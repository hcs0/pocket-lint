SyntaxCompleter
===============

The SyntaxCompleter is a gedit plugin that will suggest complete words
that match the part of a word at the cursor.

    >>> from gdp.syntaxcompleter import SyntaxComplete
    >>> data = 'This, that, and the other thing are simple words.'
    >>> print data
    This, that, and the other thing are simple words.
