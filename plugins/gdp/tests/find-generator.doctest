Finder
======

The Finder is a controller between the UI and the match gengerato. The
__init__ method requires a gedit window. An error is raised if it is not
provided.

    >>> from testing.gedithelpers import get_window
    >>> from gdp.find import Finder

    >>> finder = Finder()
    Traceback (most recent call last):
    ...
    TypeError: __init__() takes exactly 2 arguments (1 given)

    >>> window, view, document = get_window('plugins/gdp/data/snark12.txt')
    >>> finder = Finder(window)
    >>> finder
    <gdp.find.Finder ...>


