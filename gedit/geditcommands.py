"""A mock implemetation of objects."""
def load_uri(window, uri, encoding, line_pos):
    """A mock implementation of load_uri."""

def load_uris(window, uris, encoding, line_pos):
    """A mock implementation of load_uris."""
